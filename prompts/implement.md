---
description: Implement a change with focused tests and project validation
argument-hint: "<request>"
---
Implement: $@

Inspect the repository architecture, nearby implementation, dependencies/configuration, and existing tests. Infer relevant engineering skills and load them if useful. Define the smallest coherent change, implement it, and add or update behavioral tests. Run focused validation, then the project's expected local suite. Inspect the final diff and report the change, files/tests, commands and results, and any external validation or risk.
